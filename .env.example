# Never commit the real values. Testnet keys and mainnet keys are different.
BYBIT_API_KEY=qzu55WvFxsevTMdmon
BYBIT_API_SECRET=TKIdJO2Kd14P8kSKlLmghvebXlU9uXBUqzsX
BYBIT_TESTNET=true

# Grid settings (spot only)
GRID_SYMBOL=BTCUSDT
GRID_LOWER_PRICE=90000
GRID_UPPER_PRICE=110000
GRID_INTERVALS=10
GRID_QUOTE_PER_ORDER=10
GRID_POLL_SECONDS=5
GRID_ORDER_PREFIX=grid
GRID_STATE_FILE=.grid_state.json

# Orders are only submitted when this is true. Start with `plan` first.
GRID_EXECUTE=false

# Required in addition to GRID_EXECUTE=true when BYBIT_TESTNET=false.
GRID_MAINNET_CONFIRM=

